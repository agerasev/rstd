language: cpp

compiler:
  - gcc
  - clang

os: linux
dist: bionic

addons:
  apt:
    packages:
      - cmake

script:
  - mkdir build
  - cd build
  - cmake .. -DUSE_PCH=ON
  - make rstd_test
  - ./rstd_test
